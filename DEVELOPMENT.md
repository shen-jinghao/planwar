# 飞机大战课程设计开发文档

## 一、设计目的

本课程设计旨在综合运用 C++ 程序设计、模块化开发、图形界面绘制、事件循环、状态机、文件读写和小组协作等知识，完成一个功能较完整、可运行、可扩展的 2D 飞机射击小游戏。

通过本项目，主要达到以下目标：

| 目标类别 | 设计目标                                                       |
| -------- | -------------------------------------------------------------- |
| 编程能力 | 熟练使用 C++ 结构体、函数、容器、文件流和模块化源码组织方式    |
| 图形开发 | 使用 EasyX 完成窗口创建、图片绘制、文字绘制、动画和界面交互    |
| 游戏逻辑 | 实现玩家、敌机、Boss、子弹、道具、碰撞检测和关卡推进等核心玩法 |
| 工程能力 | 使用多文件工程组织代码，编写构建脚本，完成资源和源码分离管理   |
| 协作能力 | 按成员划分模块职责，完成接口配合、功能整合和文档整理           |

## 二、设计内容

本项目设计并实现一个“飞机大战”类 2D 射击游戏。玩家控制飞机躲避敌机和子弹，并自动发射子弹攻击敌人。游戏包含主菜单、局内战斗、暂停、存档、游戏结束、Boss 战、道具掉落、音效和背景音乐等功能。

### 1. 功能内容

| 功能模块 | 设计内容                                           | 涉及文件                                         |
| -------- | -------------------------------------------------- | ------------------------------------------------ |
| 程序入口 | 初始化窗口、创建游戏对象、驱动主循环和退出清理     | `main.cpp`                                       |
| 状态流程 | 主菜单、存档页、暂停、游戏结束、死亡演出、局内状态 | `State.h`、`States.*`                            |
| 游戏实体 | 玩家、敌机、Boss、子弹、道具、爆炸动画             | `Entities.*`                                     |
| 全局管理 | 游戏数据、存档读写、刷怪、击杀结算、Boss 血条      | `Game.*`                                         |
| 渲染辅助 | 去黑边绘制、背景铺满、中文文本、文字阴影           | `RenderUtil.*`                                   |
| 资源音频 | 图片缓存、资源 ID、背景音乐、一次性音效            | `Resources.*`                                    |
| 通用工具 | 窗口常量、坐标缩放、碰撞检测、键盘状态、颜色判断   | `Common.*`                                       |
| 工程配置 | 编译脚本、VS Code 调试与任务配置、EasyX 兼容适配   | `bulid.bat`、`.vscode/`、`easyx_ucrt_compat.cpp` |

### 2. 玩法内容

玩家可以通过 `WASD`、方向键或鼠标拖动控制飞机移动。玩家飞机会自动发射子弹，击毁敌机会获得分数，并有概率掉落生命、炸弹或火力强化道具。击杀数达到指定节点后会出现 Boss，Boss 拥有更高生命值，并会根据生命值变化进入不同阶段，释放更密集的弹幕。

游戏还提供 3 个存档槽，可在主菜单、暂停菜单或局内存档页中读取或保存进度。

## 三、开发环境

| 项目         | 内容            |
| ------------ | --------------- |
| 开发语言     | C++             |
| 图形库       | EasyX for MinGW |
| 编译器       | MinGW g++       |
| 构建脚本     | `bulid.bat`     |
| 目标程序     | `PlaneWar.exe`  |
| 图片资源目录 | `image/`        |
| 音频资源目录 | `music/`        |

项目可通过根目录下的 `bulid.bat` 进行编译。该脚本会检查 MinGW 编译器、EasyX 头文件和库文件，并使用 `g++` 生成 `PlaneWar.exe`。

## 四、代码规模统计

以下统计排除了 `mingw64/`、`easyx4mingw_25.9.10/`、图片、音乐、可执行文件和存档文件，仅统计项目自身核心源码。

| 文件             | 总行数 | 非空行 | 纯注释行 | 主要作用                                 |
| ---------------- | -----: | -----: | -------: | ---------------------------------------- |
| `States.cpp`     |   1184 |   1018 |       57 | 主菜单、存档页、暂停、结束、局内玩法     |
| `Entities.cpp`   |    622 |    553 |       26 | 玩家、敌机、Boss、子弹、道具、动画       |
| `Game.cpp`       |    387 |    330 |       25 | 全局状态、存档、刷怪、击杀结算、通用绘制 |
| `RenderUtil.cpp` |    298 |    256 |       14 | 图片去黑边、背景适配、中文文本绘制       |
| `Resources.cpp`  |    190 |    164 |       11 | 图片缓存、音效和背景音乐                 |
| `Entities.h`     |    174 |    142 |       29 | 实体结构与接口声明                       |
| `Game.h`         |     96 |     78 |       23 | 游戏上下文与存档数据声明                 |
| `Resources.h`    |     93 |     77 |       13 | 资源路径、资源 ID 和资源管理器声明       |
| `Common.h`       |     58 |     49 |       12 | 常量、缩放函数、碰撞和输入工具声明       |
| `main.cpp`       |     58 |     45 |        3 | 程序入口和主循环                         |
| `Common.cpp`     |     37 |     31 |        7 | 通用工具函数实现                         |
| `State.h`        |     29 |     25 |        7 | 状态机基类                               |
| `RenderUtil.h`   |     27 |     25 |       12 | 渲染工具接口声明                         |

核心源码合计约 3253 行，其中非空行约 2793 行。

## 五、小组成员承担工作

本项目采用按模块分工、统一整合的方式完成。各成员负责相对独立的功能模块，并通过头文件接口与其他模块协作。

| 成员   | 负责模块               | 主要承担工作                                                               |
| ------ | ---------------------- | -------------------------------------------------------------------------- |
| 张思媛 | `Game.h`、`Game.cpp`   | 全局游戏数据、存档读写、敌人生成、Boss 管理、击杀结算、掉落、世界绘制      |
| 李欣雅 | `RenderUtil.*`         | 去黑边绘制、背景等比铺满、UTF-8 中文文本绘制、文字阴影、底层渲染工具封装   |
| 申璟皓 | 其余核心模块与工程配置 | 程序入口、状态机流程、实体系统、资源音频管理、通用工具、编译配置和整体整合 |

### 1. 张思媛承担工作

张思媛主要负责 `Game.h` 和 `Game.cpp`。该部分是项目的全局调度核心，负责保存游戏运行时共享数据，并向各个状态提供统一接口。

具体工作包括：设计 `GameSaveData` 存档数据结构；实现游戏重置、存档保存、存档读取和读档数据校验；实现普通敌机和 Boss 的生成；实现 Boss 查询接口；实现敌机击杀后的分数、击杀数、关卡、掉落和爆炸动画结算；实现背景绘制、世界实体绘制和 Boss 血条绘制。

### 2. 李欣雅承担工作

李欣雅主要负责 `RenderUtil.h` 和 `RenderUtil.cpp`。该部分是项目的底层绘制辅助模块，负责封装 EasyX 中较复杂、重复使用的渲染逻辑。

具体工作包括：实现图片外侧黑边遮罩算法；实现普通居中绘制和 180 度旋转绘制；实现背景图片等比裁剪铺满窗口；实现 UTF-8 字符串转宽字符并输出中文文本；实现文字宽高计算、阴影文本绘制和黑白文字样式设置。

### 3. 申璟皓承担工作

申璟皓负责除 `Game.*` 和 `RenderUtil.*` 外的其余核心模块与工程配置。该部分覆盖游戏运行入口、状态机流程、实体行为、资源音频、通用工具和整体功能整合。

具体工作包括：实现 `main.cpp` 主循环；实现 `State.h` 状态机基类；实现 `States.*` 中的主菜单、制作名单、存档页、暂停菜单、游戏结束、死亡演出和局内游戏逻辑；实现 `Entities.*` 中的玩家、敌机、Boss、子弹、道具和动画效果；实现 `Resources.*` 中的资源路径、图片缓存、背景音乐和音效播放；实现 `Common.*` 中的碰撞、按键、文件检测和颜色判断；维护 `.vscode/` 配置、构建脚本和 EasyX 兼容代码。

## 六、总体架构设计

项目采用“主循环 + 状态机 + 实体系统 + 资源系统 + 渲染工具”的结构。

程序入口位于 `main.cpp`。程序启动后初始化 EasyX 窗口，开启批量绘图，创建 `Game` 对象并进入主菜单状态。主循环中持续计算每帧时间 `dt`，再调用当前状态的 `Update(dt)` 和 `Render()` 完成逻辑更新与画面绘制。

状态机基类定义在 `State.h` 中，每个具体状态都实现 `Enter()`、`Exit()`、`Update()` 和 `Render()`。主菜单、局内游戏、暂停菜单、存档页、游戏结束界面和玩家死亡演出均被拆分成不同状态，降低了界面流程之间的耦合。

实体系统定义在 `Entities.*` 中，包括玩家、敌机、Boss、子弹、掉落道具和动画效果。实体负责自身移动、射击、受伤、绘制等局部行为。

资源系统定义在 `Resources.*` 中，负责图片按需加载、缓存释放、背景音乐播放和音效播放。渲染工具定义在 `RenderUtil.*` 中，进一步封装 EasyX 的底层绘图细节，使其它模块可以直接调用统一的绘制接口。

## 七、成员负责模块设计说明

### 1. 张思媛负责模块：Game.h 与 Game.cpp

`Game` 是游戏运行时的全局上下文，也是各个状态共享的数据中心。它保存玩家、敌人、道具、动画、分数、关卡、击杀数、背景编号、Boss 触发进度和提示文本等信息。

| 成员                                   | 说明                       |
| -------------------------------------- | -------------------------- |
| `ResourceManager resources`            | 图片资源缓存和读取入口     |
| `std::unique_ptr<State> current`       | 当前运行状态               |
| `Player player`                        | 玩家飞机对象               |
| `std::vector<Enemy> enemies`           | 当前场上敌人和 Boss        |
| `std::vector<Item> items`              | 当前场上掉落道具           |
| `std::vector<AnimationEffect> effects` | 当前播放中的爆炸动画       |
| `score`、`kills`、`level`              | 分数、击杀数和关卡         |
| `backgroundIndex`                      | 当前背景编号               |
| `spawnTimer`、`spawnInterval`          | 普通敌机刷新计时           |
| `nextBossKills`                        | 下一次 Boss 出现所需击杀数 |
| `noticeText`、`noticeTimer`            | 临时提示文本及显示时间     |

该模块的关键设计是把跨状态共享的数据和行为集中到 `Game` 中，避免主菜单、暂停、局内游戏等状态重复实现存档、刷怪、结算和绘制逻辑。存档读取后通过 `ClampLoadedData()` 修正异常数据，提高了程序鲁棒性。

### 2. 李欣雅负责模块：RenderUtil.h 与 RenderUtil.cpp

`RenderUtil` 是项目的底层渲染辅助模块，主要目标是封装 EasyX 中重复、复杂或容易出错的绘制逻辑。

该模块实现了图片外侧黑边去除、居中绘制、旋转绘制、背景等比铺满、中文文本输出和文字阴影。黑边处理通过从图片边缘开始搜索连通近黑色区域生成遮罩，并对遮罩结果进行缓存，既解决素材黑底问题，又避免每帧重复计算。

中文绘制方面，模块使用 `MultiByteToWideChar` 将 UTF-8 字符串转换成宽字符，再通过 Windows GDI 输出文本，解决中文菜单、HUD 和提示信息乱码问题。

### 3. 申璟皓负责模块：其余核心模块

申璟皓负责 `main.cpp`、`State.h`、`States.*`、`Entities.*`、`Resources.*`、`Common.*`、`easyx_ucrt_compat.cpp`、`.vscode/` 配置和构建脚本等。

该部分的重点是实现完整游戏流程和局内玩法。状态机部分负责主菜单、存档、暂停、结束和局内游戏之间的切换；实体部分负责玩家自动射击、敌机刷新、Boss 阶段弹幕、道具掉落、碰撞检测和爆炸动画；资源部分负责图片缓存和音频播放；通用工具部分负责坐标、碰撞、输入和文件判断等基础能力。

## 八、设计结果分析

### 1. 功能完成情况

| 功能     | 设计结果                                             |
| -------- | ---------------------------------------------------- |
| 主菜单   | 已实现开始游戏、读取存档、退出、背景切换、制作名单   |
| 局内操作 | 已实现键盘移动、鼠标拖动、自动射击、暂停、炸弹       |
| 敌机系统 | 已实现普通敌机、强化敌机和 Boss                      |
| Boss 战  | 已实现 Boss 血条、阶段变化、密集弹幕和特殊子弹       |
| 道具系统 | 已实现生命、炸弹、火力强化三类道具                   |
| 存档系统 | 已实现 3 个存档槽、保存、读取和存档摘要展示          |
| 关卡推进 | 已实现随击杀数提升关卡和 Boss 节点触发               |
| 音频系统 | 已实现菜单音乐、背景音乐和多种音效                   |
| 渲染系统 | 已实现图片缓存、背景铺满、去黑边绘制、中文文本和阴影 |
| 游戏结束 | 已实现玩家死亡演出、游戏结束界面和重新开始           |

### 2. 运行效果分析

从功能实现情况看，项目已经形成完整的游戏闭环：玩家可以从主菜单开始游戏，在局内进行移动、射击、拾取道具、使用炸弹和挑战 Boss，死亡后进入结束界面，并可重新开始或读取存档。

从结构设计看，项目将界面状态、实体行为、全局数据、资源管理和渲染工具拆分到不同模块中，各模块职责比较清晰。`Game` 作为共享上下文连接状态机与实体系统，`RenderUtil` 作为底层绘制工具为界面和实体提供统一绘制能力。

从用户体验看，项目支持键盘和鼠标两种控制方式，提供背景音乐、音效、爆炸动画、Boss 血条、HUD 信息和临时提示文本，整体操作流程较完整。

## 九、主要创新点

| 创新点         | 具体说明                                                       |
| -------------- | -------------------------------------------------------------- |
| 多状态流程组织 | 使用状态机拆分主菜单、局内、暂停、存档、结束和死亡演出         |
| Boss 阶段弹幕  | Boss 根据血量进入不同阶段，移动速度、射击间隔和弹幕密度会变化  |
| 三槽位存档系统 | 支持多个存档文件，并在读取时对异常数据进行范围修正             |
| 图片去黑边算法 | 使用边缘连通近黑色区域遮罩，解决带黑底素材绘制时出现黑边的问题 |
| UTF-8 中文绘制 | 将 UTF-8 文本转换为宽字符输出，保证中文菜单和 HUD 正常显示     |
| 音效别名轮换   | 播放音效时轮换多个 MCI alias，减少短时间连续音效被截断的问题   |
| 键鼠双控制方式 | 同时支持键盘方向控制和鼠标拖动控制，提高玩家操作灵活性         |

## 十、存在问题及解决方案

| 存在问题              | 问题表现                                      | 已采用的解决方案                                              |
| --------------------- | --------------------------------------------- | ------------------------------------------------------------- |
| 图片素材存在黑底      | 飞机、子弹、道具直接绘制时会出现黑色边缘      | 在 `RenderUtil` 中实现外侧黑色连通区域遮罩并跳过对应像素      |
| 中文文本可能乱码      | EasyX 默认输出中文时可能受编码影响            | 使用 UTF-8 转宽字符，再通过 Windows GDI 输出中文文本          |
| 音效连续播放易截断    | 多个音效短时间播放时可能互相覆盖              | 使用多个 MCI alias 轮换播放一次性音效                         |
| 存档数据可能异常      | 手动修改或损坏存档可能导致位置越界、生命非法  | 设置存档版本标识，并在读取后调用 `ClampLoadedData()` 修正范围 |
| 按键长按重复触发      | 暂停、炸弹、菜单点击等操作可能被连续执行      | 使用 `pPrev`、`bPrev`、`lbuttonPrev` 等变量判断按键边沿       |
| 实体清理时机复杂      | 子弹、敌机、道具、动画都需要在合适时机移除    | 使用 `alive` 标记配合 `remove_if` 统一清理无效对象            |
| Boss 与普通敌机差异大 | Boss 移动、血量、弹幕和奖励逻辑与普通敌机不同 | 通过 `type`、`IsBoss()`、`Phase()` 等接口区分行为和结算规则   |

后续仍可继续优化的方向包括：增加更多敌机类型和 Boss 类型；加入难度选择；增加排行榜；优化素材透明通道；将存档格式改为更结构化的数据格式；进一步拆分 `States.cpp` 中较集中的局内逻辑。

## 十一、开发难度分析

整体开发难度为中等偏上。项目虽然是 2D 小游戏，但包含完整的状态切换、实体管理、存档、音频、渲染和 Boss 逻辑，模块之间存在较多协作关系。

| 模块           | 难度 | 原因                                                 |
| -------------- | ---- | ---------------------------------------------------- |
| 状态机流程     | 较高 | 主菜单、暂停、存档、局内、结束等状态之间需要稳定切换 |
| 局内玩法       | 较高 | 需要同时处理刷怪、移动、射击、碰撞、道具、炸弹和清理 |
| `Game.*`       | 较高 | 承担全局数据中心职责，需要兼顾存档、生成、结算和绘制 |
| `RenderUtil.*` | 较高 | 涉及像素级绘制、遮罩缓存、背景裁剪和 UTF-8 中文输出  |
| 资源与音频     | 中等 | 需要处理图片缓存、音效播放别名和文件存在性检查       |
| 基础实体       | 中等 | 实体种类较多，但结构清晰，主要难点在行为细节         |

## 十二、课程设计总结

本项目完成了一个功能较完整的飞机射击小游戏。代码结构上使用状态机拆分游戏流程，使用实体系统组织玩家、敌机、子弹、道具和动画，使用资源系统统一加载图片与音频，并通过渲染工具解决素材显示和中文绘制问题。

张思媛负责的 `Game.h` 与 `Game.cpp` 是项目的全局调度核心，承担了存档、重置、刷怪、Boss 管理、击杀结算、掉落和世界绘制等关键功能。李欣雅负责的 `RenderUtil.h` 与 `RenderUtil.cpp` 为整个项目提供了稳定的绘制基础，解决了黑边素材、背景适配和中文显示等实际开发问题。申璟皓负责其余核心模块和工程配置，完成了程序入口、状态机流程、实体系统、资源音频管理、通用工具和整体功能整合。

从最终效果看，项目不仅实现了基础飞机大战玩法，还加入了 Boss 战、道具、存档、音效和多界面交互，满足课程设计对综合编程能力、模块划分能力和小组协作能力的要求。
